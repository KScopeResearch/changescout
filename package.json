{
  "name": "changescout",
  "version": "0.0.0",
  "private": true,
  "description": "PJ2 Lead永続化のS3移行（scripts/generator/leads/backends/s3-backend.js）とSES送信のAWS認証情報解決（scripts/generator/leads/ses-client.js）でのみ使用するnpm依存を管理する。プロジェクト全体は引き続きNode.js標準モジュールのみで実装する方針（scripts/generator/README.md参照）であり、これらの依存はAWS SDKアクセス専用のスコープに限定する。",
  "license": "UNLICENSED",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/credential-provider-node": "^3.972.78"
  }
}
