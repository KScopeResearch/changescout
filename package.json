{
  "name": "changescout",
  "version": "0.0.0",
  "private": true,
  "description": "PJ2 Lead永続化のS3移行（scripts/generator/leads/backends/s3-backend.js）でのみ使用するnpm依存を管理する。プロジェクト全体は引き続きNode.js標準モジュールのみで実装する方針（scripts/generator/README.md参照）であり、この依存はS3アクセス専用のスコープに限定する。",
  "license": "UNLICENSED",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
